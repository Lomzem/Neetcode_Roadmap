class Solution:
    def longestConsecutive(self, nums: list[int]) -> int:
        sorted_nums = sorted(nums)
        longest_length = 0 # int

        lag_array = []
        for i, num in sorted_nums:
            if num == s
            lag_array.append(num)
